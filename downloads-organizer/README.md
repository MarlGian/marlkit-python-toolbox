# Downloads Organizer

A simple Python script that automatically organizes files in the Downloads folder into categorized subfolders.

## Features
- Sorts documents, images, videos, executables, archives, and audio files
- Automatically creates missing folders
- Uses pathlib for clean path handling

## Requirements
- Python 3.10+

## Usage
Run:
python organizer.py
